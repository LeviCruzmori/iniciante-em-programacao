// modulo 6
/*
function pulaLinha() {

    document.write("<br>");
}

function mostra(frase) {
    document.write(frase);
    pulaLinha();
}


// implementando o comando de converção de string para number (parseInt)
let vitorias = parseInt (prompt("entre com o numero de vitorias."));
let empates = parseInt(prompt("entre com o numero de empates"));
let pontos = vitorias * 3 + empates;

if(pontos > 28){
    mostra ("  seu time esta melhor que ano passado " + pontos);
}
if (pontos < 28){
    mostra ("  seu time esta pior que o ano passado " + pontos);
}
if (pontos == 28){
    mostra (" seu time esta igual o ano passado " + pontos);
}

mostra (" os pontos do seu time é " + pontos);

mostra("fim")

*/

